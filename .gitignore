node_modules/
.env
.env.local
.DS_Store
bot-messages.json
bot-results.json
# NOTE: .github/bot-state.json is intentionally tracked so the read offset
# (lastUpdateId) survives across stateless GitHub Actions runs.
*.pyc
__pycache__/
.pytest_cache/
